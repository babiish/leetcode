class Solution {
    public int[] smallerNumbersThanCurrent(int[] nums)
     {
                // 8,1,2,2,3
         int n=nums.length;
         int arr[]=new int[n];
                
         for(int i=0;i<nums.length;i++)
            for(int j=0;j<nums.length;j++)
               if(nums[i]>nums[j])
                arr[i]++;
    
        return arr;
    }
}
